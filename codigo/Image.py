class Image:
    def __init__(self, idImage, img, pose, coord, resize):
        self.idImage      = idImage
        self.imgOrig      = img
        self.pose         = pose
        self.coord        = coord
        self.resize       = resize
        
